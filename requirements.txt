openai>=1.60
pydantic>=2.9
fastapi>=0.115
uvicorn>=0.32
requests>=2.32
rapidfuzz>=3.10
praw>=7.8
pytest>=8.3
python-dotenv>=1.0
# Lakebase. psycopg3 is what the Databricks Apps tutorial requires, and the
# SDK floor is where generate_database_credential() lands - the Apps runtime
# preinstalls 0.33.0, which is too old.
psycopg[binary,pool]>=3.2
databricks-sdk>=0.81.0

# The audio stage. None of these were declared, so `src/audio/` ran only on the
# machine that vendored it — a fresh clone reached the voice pipeline and died
# on `No module named 'yaml'`, four imports before it would have died on the
# next one.
#
# pydub shells out to ffmpeg for anything that is not a wav. If mastering fails
# with "Couldn't find ffmpeg or avconv", that is the missing piece and it is a
# system package, not a pip one.
PyYAML>=6.0
pydub>=0.25
jsonschema>=4.20
sarvamai>=0.1
elevenlabs>=1.0
